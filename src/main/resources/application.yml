spring.application.name: WorkHub Api Gateway

server:
  port: 8085
spring:
  application:
    name: API-GATEWAY-SERVICE
  cloud:
    gateway:
      routes:
        - id: User Service
          uri: http://localhost:8081
          predicates:
            - Path=/v1/api/user/**